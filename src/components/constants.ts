// Default constants for the sorting visualizer
export const DEFAULT_ALGO = "insertion" as const;
export const DEFAULT_SIZE = 48;
export const DEFAULT_SPEED_MS = 72;

// Minimum and maximum limits for array size
export const MIN_SIZE = 5;
export const MAX_SIZE = 128;

// Minimum and maximum limits for speed
export const MIN_SPEED_MS = 5;
export const MAX_SPEED_MS = 256;
